# Allure configuration
# Specifies the directory where Allure writes raw JSON test results.
# The allure-maven-plugin reads from this path to generate the HTML report.
allure.results.directory=target/allure-results